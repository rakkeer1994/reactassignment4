import React from 'react'

const Home = () => {
  return (
    <div> 
      <h1>this is about home page</h1>
      <p>  run `npm fund` for details

8 vulnerabilities (6 moderate, 2 high)

To address all issues (including breaking changes), run:
  npm audit fix --force

Run `npm audit` for details.</p>
    </div>
  )
}

export default Home